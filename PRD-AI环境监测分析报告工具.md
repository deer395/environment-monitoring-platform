# PRD：AI 辅助环境监测数据分析与报告生成工具

## 1. 摘要

本产品是一个本地 Streamlit 应用，用于帮助环境科研人员快速完成监测数据分析、图表生成和报告初稿生成。系统只使用合成或脱敏数据；所有数值结果必须由确定性的 Python 代码计算；AI 只能基于结构化统计结果生成报告文字，不能编造环境机理或因果解释。

第一版聚焦省级环境数据分析项目中的高重复工作：按站点和变量生成原始时间序列、平均时间序列、异常图，并计算最大值、最小值、均值、标准差、日变化幅度等统计指标。

## 2. 背景与目标

### 2.1 背景

研究组当前需要对多个监测站点和多个环境变量反复做相同分析。常见流程包括数据读取、清洗、分组、绘图、统计计算和撰写报告描述。该流程耗时、重复、容易出错，也难以保证不同人员输出格式一致。

AI 可以提高报告文字生成效率，但环境数据分析属于严谨科研场景，不能让 AI 直接计算指标、解释机制或创造结论。因此，系统需要把“确定性计算”和“AI 文本生成”严格分开。

### 2.2 产品目标

1. 将站点、变量、时间范围维度下的常规分析流程标准化。
2. 减少人工制图、复制统计值和撰写基础描述的时间。
3. 保证所有数值结果可复现、可追踪、可导出。
4. 降低 AI 幻觉风险，让 AI 只根据已计算的结构化结果写报告文字。

### 2.3 首版成功判断

首版成功不是“AI 自动完成科研解释”，而是“研究人员能更快、更稳地生成可核查的图表和报告初稿”。

## 3. 目标用户

### 3.1 主要用户

1. 环境科学研究生  
   需要批量处理站点和变量数据，生成论文、课题汇报或阶段报告中的基础图表和统计描述。

2. 项目分析人员  
   负责省级或区域级环境监测项目的数据整理、质量检查、图表输出和报告初稿。

3. 课题负责人或导师  
   需要快速查看不同站点、变量、时间范围的分析结果，检查趋势、异常和统计摘要。

### 3.2 次要用户

1. 数据管理员  
   需要检查输入数据字段、缺失情况、异常值标记和导出结果。

2. 报告编写人员  
   需要基于已确认的统计结果生成结构化文字段落。

## 4. 核心用户问题

1. 重复劳动多  
   同一套分析需要在多个站点、变量和时间段上反复执行。

2. 输出不一致  
   不同人员使用不同脚本或模板，图表样式、统计口径和文字描述不统一。

3. 人工复制容易出错  
   把最大值、最小值、均值、标准差等指标从代码结果复制到报告时，容易漏填或错填。

4. AI 使用边界不清  
   如果让 AI 直接分析原始数据或解释机制，可能产生虚构结论，影响科研可信度。

5. 数据安全要求高  
   项目数据可能涉及未公开监测数据，需要本地处理，并优先使用合成或脱敏数据。

6. 复现成本高  
   报告中的图表和数字需要能追溯到输入数据、筛选条件和计算方法。

## 5. 价值主张

### 5.1 Who：服务谁

服务需要批量分析环境监测数据的科研团队，尤其是省级项目中的研究生、数据分析人员和报告撰写人员。

### 5.2 Why：核心需求

他们需要在保证结果可复现和可核查的前提下，快速完成站点和变量维度的图表、统计和报告初稿。

### 5.3 What Before：当前状态

用户通常依靠分散的 Python 脚本、Excel、手工复制和人工写作。每次换站点、变量或时间范围，都要重复运行、保存图表、整理统计值和写描述。

### 5.4 How：产品方案

本地 Streamlit 应用提供统一上传入口、字段映射、筛选控件、确定性统计计算、标准图表生成、结构化结果导出和受控 AI 报告生成。

### 5.5 What After：改进结果

用户能在一个界面中选择站点、变量和时间范围，一键生成图表、统计表和报告草稿。导师或负责人可以追溯每个数字的计算来源。

### 5.6 Alternatives：替代方案

替代方案包括手写 Python 脚本、Excel 模板、Jupyter Notebook、通用 BI 工具和直接使用大模型。相比这些方案，本产品更适合本地、批量、可复现、AI 受控的环境监测报告工作流。

## 6. Jobs-to-be-Done

1. 当我拿到一个省级项目的多站点监测数据时，我想快速完成字段检查和基础清洗，以便确认数据能否进入分析流程。

2. 当我要分析某个站点的某个环境变量时，我想一键生成原始时间序列、平均时间序列和异常图，以便快速了解该变量的时间变化。

3. 当我要写报告统计描述时，我想自动获得最大值、最小值、均值、标准差和日变化幅度，以便减少手工计算和复制错误。

4. 当我要批量处理多个站点和变量时，我想复用同一套分析模板，以便保证图表和统计口径一致。

5. 当我要使用 AI 生成报告文字时，我想让 AI 只读取已经计算好的结构化结果，以便降低虚构机制和错误结论的风险。

6. 当导师或项目负责人审阅结果时，我想导出图表、统计表、参数配置和报告草稿，以便检查和复现。

## 7. MVP 范围

### 7.1 包含范围

1. 本地 Streamlit 应用。
2. 支持上传 CSV 或 Excel 格式的合成或脱敏环境监测数据。
3. 支持字段映射：时间、站点、变量、数值。
4. 支持按站点、变量、时间范围筛选。
5. 支持基础数据质量检查：缺失值、重复记录、非数值记录、时间解析失败。
6. 生成原始时间序列图。
7. 生成平均时间序列图，首版至少支持小时均值和日均值。
8. 生成异常图，首版以规则阈值或 z-score 方式标记异常。
9. 计算统计指标：最大值、最小值、均值、标准差、日变化幅度。
10. 展示结构化统计表。
11. 导出图表、统计 CSV、分析配置 JSON 和 Markdown 报告草稿。
12. AI 基于结构化统计 JSON 生成报告文字。
13. 报告中标明“AI 文字仅基于已计算统计结果生成，不包含未经验证的环境机制解释”。

### 7.2 首版用户流程

1. 用户打开本地 Streamlit 应用。
2. 上传合成或脱敏数据文件。
3. 完成字段映射并查看数据检查结果。
4. 选择站点、变量和时间范围。
5. 点击生成分析。
6. 查看图表和统计表。
7. 点击生成报告草稿。
8. 导出结果包。

## 8. 非目标

1. 不连接真实生产数据库。
2. 不处理未脱敏的敏感项目数据。
3. 不让 AI 直接读取原始数据并自行计算数值。
4. 不让 AI 生成未经统计结果支持的环境机制解释、污染来源判断或治理建议。
5. 不做法规合规结论或执法判断。
6. 不做多用户权限系统。
7. 不做云端部署。
8. 不做复杂机器学习预测模型。
9. 不替代科研人员的专业解释和审稿判断。
10. 不承诺自动生成可直接发表的论文级结论。

## 9. 功能需求

### 9.1 数据导入

1. 系统必须支持 CSV 文件上传。
2. 系统应支持 Excel 文件上传。
3. 系统必须提示用户只能上传合成或脱敏数据。
4. 系统必须显示文件行数、列数和字段列表。
5. 系统必须支持用户选择时间列、站点列、变量列和数值列。

### 9.2 数据校验

1. 系统必须检查时间列是否能被解析为时间格式。
2. 系统必须检查数值列是否能转换为数值类型。
3. 系统必须显示缺失值数量和比例。
4. 系统必须显示重复记录数量。
5. 系统必须在关键字段缺失时阻止继续分析。

### 9.3 筛选与配置

1. 用户必须能选择一个或多个站点。
2. 用户必须能选择一个或多个变量。
3. 用户必须能设置时间范围。
4. 用户必须能选择平均方式：小时均值或日均值。
5. 用户必须能选择异常检测方法：固定阈值或 z-score。
6. 用户必须能查看当前分析配置。

### 9.4 图表生成

1. 系统必须生成原始时间序列图。
2. 系统必须生成平均时间序列图。
3. 系统必须生成异常图，并突出显示异常点。
4. 图表必须包含标题、坐标轴标签、单位占位字段和图例。
5. 图表必须支持 PNG 导出。

### 9.5 统计计算

1. 系统必须用 Python 确定性代码计算所有数值。
2. 系统必须计算最大值、最小值、均值和标准差。
3. 系统必须计算日变化幅度。建议定义为同一日内最大值减最小值，再对筛选期内每日振幅进行汇总。
4. 系统必须显示指标定义，避免口径不清。
5. 系统必须导出统计表 CSV。

### 9.6 AI 报告生成

1. AI 输入只能是结构化统计结果、图表清单、筛选配置和用户选择的报告语气。
2. AI 不得直接访问原始数据文件。
3. AI 不得生成没有结构化统计结果支持的数值。
4. AI 不得解释污染成因、环境机制或政策含义，除非用户提供经过确认的外部解释材料。MVP 默认不支持该能力。
5. AI 生成内容必须包含可核查数字，并引用对应统计字段。
6. 系统必须在报告草稿中显示 AI 使用边界说明。

### 9.7 导出

1. 用户必须能导出 Markdown 报告草稿。
2. 用户必须能导出统计 CSV。
3. 用户必须能导出分析配置 JSON。
4. 用户必须能导出图表 PNG。
5. 导出文件名应包含站点、变量、时间范围和生成时间。

## 10. 非功能需求

1. 本地运行  
   MVP 必须能在研究组普通电脑上本地运行。

2. 可复现  
   同一输入数据和同一配置必须产生相同统计结果。

3. 可追溯  
   报告中的每个数值都应能在统计表中找到来源。

4. 安全边界  
   默认不上传原始数据到云端。若未来接入云端模型，必须只发送结构化统计结果。

5. 性能  
   对 10 万行以内数据，常规筛选、统计和绘图应在可接受时间内完成，目标为 10 秒内完成单次分析。

6. 易用性  
   非工程背景用户应能在 10 分钟内完成一次完整分析。

7. 稳定性  
   输入字段错误、时间解析失败、空筛选结果等情况必须给出清楚提示。

8. 可维护性  
   统计计算函数应与 UI 代码分离，便于测试。

9. 测试性  
   核心统计函数必须有单元测试，尤其是均值、标准差、日变化幅度和异常标记。

## 11. 用户故事

### 用户故事 1：上传并映射数据

作为环境数据分析人员，我想上传合成或脱敏数据并映射关键字段，以便系统知道如何读取时间、站点、变量和数值。

验收标准：
1. 用户可以上传 CSV 文件。
2. 用户可以选择时间列、站点列、变量列和数值列。
3. 如果时间列无法解析，系统显示错误并阻止分析。
4. 如果数值列无法转换，系统显示错误并阻止分析。
5. 系统显示数据行数、列数和缺失值概览。

### 用户故事 2：选择分析对象

作为研究生，我想选择站点、变量和时间范围，以便只分析当前报告需要的部分数据。

验收标准：
1. 用户可以从下拉列表选择站点。
2. 用户可以从下拉列表选择变量。
3. 用户可以设置开始时间和结束时间。
4. 如果筛选结果为空，系统显示清楚提示。
5. 系统显示当前筛选后的记录数。

### 用户故事 3：生成标准图表

作为报告编写人员，我想一键生成原始时间序列、平均时间序列和异常图，以便快速插入报告。

验收标准：
1. 系统生成原始时间序列图。
2. 系统生成小时均值或日均值时间序列图。
3. 系统生成异常点图。
4. 每张图都有标题、坐标轴标签和图例。
5. 用户可以导出 PNG 图表。

### 用户故事 4：计算统计指标

作为项目分析人员，我想自动计算最大值、最小值、均值、标准差和日变化幅度，以便减少手工计算错误。

验收标准：
1. 系统显示所有统计指标。
2. 指标由 Python 确定性代码计算。
3. 同一输入和配置重复运行结果一致。
4. 用户可以查看指标定义。
5. 用户可以导出统计 CSV。

### 用户故事 5：生成受控 AI 报告草稿

作为报告撰写人员，我想让 AI 基于统计结果生成报告文字，以便节省初稿写作时间。

验收标准：
1. AI 输入不包含原始数据。
2. AI 输入只包含结构化统计结果和分析配置。
3. AI 报告中的数值必须来自结构化统计结果。
4. 报告不包含未经提供的环境机制解释。
5. 报告包含 AI 使用边界说明。

### 用户故事 6：导出可复现结果包

作为课题负责人，我想导出图表、统计表、配置和报告草稿，以便审阅和复现结果。

验收标准：
1. 用户可以导出 Markdown 报告。
2. 用户可以导出统计 CSV。
3. 用户可以导出配置 JSON。
4. 用户可以导出图表 PNG。
5. 导出文件名包含站点、变量和时间范围。

## 12. 验收标准总表

1. 使用合成或脱敏样例数据可以完成完整流程。
2. 所有数值结果均由 Python 函数计算，不由 AI 计算。
3. AI 无法访问原始数据文件。
4. AI 生成文本不包含虚构机制、污染来源判断或未经验证的因果解释。
5. 图表至少包括原始时间序列、平均时间序列和异常图。
6. 统计表至少包括最大值、最小值、均值、标准差和日变化幅度。
7. 同一数据和配置重复运行，统计结果一致。
8. 用户可以导出图表、统计表、配置和报告草稿。
9. 错误输入能得到清楚提示，而不是程序崩溃。
10. 核心统计函数通过单元测试。

## 13. 风险假设

| 风险类型 | 假设 | 可能出错的地方 | 信心 | 测试方式 |
|---|---|---|---|---|
| 价值 | 用户愿意把现有脚本迁移到 Streamlit 工具 | 用户更信任自己的 Notebook，不愿迁移 | 中 | 用 2-3 名真实用户试用样机，比较完成一次分析所需时间 |
| 价值 | 标准图表和统计指标覆盖大多数报告需求 | 不同课题有更多特殊指标 | 中 | 收集最近 3 份报告，统计常用图表和指标 |
| 可用性 | 非工程用户能理解字段映射和配置 | 字段命名复杂，用户不知道如何选择 | 中 | 做一次 30 分钟可用性测试，观察是否需要帮助 |
| 可用性 | AI 边界说明能被用户理解 | 用户仍可能把 AI 文本当作最终科研解释 | 中 | 在报告草稿中加入明显提示，并让用户复述理解 |
| 可行性 | 本地电脑能处理常见数据规模 | 数据过大导致 Streamlit 卡顿 | 中 | 用 1 万、10 万、50 万行合成数据做性能测试 |
| 可行性 | 统计逻辑可以标准化 | 不同变量的单位、频率和异常规则差异大 | 中 | 为每类变量建立配置模板 |
| 可行性 | AI 能稳定遵守“只基于统计结果写作” | 模型可能扩写出机制解释 | 低到中 | 使用严格提示词、输出校验和人工审阅流程 |
| 可行性 | 日变化幅度定义能被团队接受 | 不同成员对 diurnal amplitude 口径不同 | 中 | 在 PRD 和 UI 中固定定义，并请负责人确认 |
| 合规 | 脱敏数据足以支持开发和演示 | 脱敏后数据分布失真，难以测试边界情况 | 中 | 构造合成数据集覆盖缺失、异常、不同频率和多站点 |
| 维护 | 统计函数与 UI 分离能降低维护成本 | 快速开发时容易把逻辑写进 UI | 高 | 代码评审要求统计函数独立，并配单元测试 |

## 14. 产品指标

### 14.1 北极星指标

有效分析报告生成率：用户在一次会话中成功导出包含图表、统计表、配置和报告草稿的结果包次数 / 开始分析会话次数。

### 14.2 输入指标

| 指标 | 定义 | 数据来源 | 目标 | 警戒线 |
|---|---|---|---|---|
| 完整流程完成率 | 完成上传、配置、分析、导出的会话数 / 开始会话数 | 本地使用日志 | MVP 试用达到 70% | 低于 50% |
| 单次分析耗时 | 从点击生成分析到图表和统计表完成的时间 | 应用日志 | 10 万行内小于 10 秒 | 超过 30 秒 |
| 报告草稿采用率 | 被用户导出的 AI 报告草稿数 / 生成草稿数 | 本地使用日志 | 60% | 低于 30% |
| 人工修改负担 | 用户认为报告草稿需要大改的比例 | 用户反馈 | 低于 30% | 高于 50% |
| 批量复用次数 | 同一配置模板被用于多个站点或变量的次数 | 配置日志 | 持续增长 | 无复用 |

### 14.3 健康指标

| 指标 | 定义 | 目标 |
|---|---|---|
| 统计测试通过率 | 核心统计单元测试通过数 / 总测试数 | 100% |
| AI 越界率 | AI 文本出现未提供机制解释或新增数值的次数 / 生成次数 | 0 |
| 导出成功率 | 成功导出结果包次数 / 尝试导出次数 | 95% 以上 |
| 崩溃率 | 出现未捕获异常的会话数 / 总会话数 | 低于 2% |
| 用户信任评分 | 用户对数值可信度和可追溯性的评分 | 4/5 以上 |

### 14.4 评估方式

1. 用合成数据集做自动化回归测试。
2. 用 3-5 名组内用户做可用性测试。
3. 对比旧流程和新流程完成同一任务的耗时。
4. 人工审查 20 份 AI 报告草稿，记录是否出现新增数值或机制解释。
5. 每周复盘错误日志、用户反馈和导出结果。

## 15. 发布计划

### 15.1 MVP

本地 Streamlit 应用，支持单文件上传、字段映射、筛选、基础图表、基础统计、受控 AI 报告草稿和结果导出。

### 15.2 下一阶段

1. 支持批量站点和变量自动循环生成结果包。
2. 支持更多统计口径和变量模板。
3. 支持图表样式模板。
4. 支持报告章节模板。
5. 支持本地模型或私有化模型配置。
6. 支持更完整的数据质量报告。

### 15.3 未来可能方向

1. 团队共享模板库。
2. 与内部数据仓库连接。
3. 增加人工审核流程。
4. 增加报告版本管理。
5. 增加更复杂的统计检验，但仍由确定性代码执行。

## 16. 关键原则

1. Python 负责计算，AI 负责表达。
2. 先结构化结果，再生成文字。
3. 先本地可复现，再考虑自动化扩展。
4. 宁可少解释，也不编造机制。
5. 每个报告数字都必须可追溯。

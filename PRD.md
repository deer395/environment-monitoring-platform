# PRD：AI 辅助环境监测数据分析与报告生成工具 V1

## 1. 摘要

本产品 V1 是一个 2-3 周内可完成的本地 Streamlit 演示应用。它帮助研究人员用合成或脱敏的环境监测数据，完成单文件、单站点、单变量的基础分析、图表生成和报告草稿生成。

V1 的核心原则是：所有数值结果必须由确定性的 MATLAB 或 Python 逻辑计算；AI 只能基于已经计算好的结构化统计结果生成中文报告文字；AI 不得读取原始数据，不得发明数值，不得编造环境机制、污染来源或因果解释。

## 2. 目标用户

### 2.1 V1 主要用户

1. 环境科学研究生  
   需要快速查看一个站点、一个变量在某段时间内的变化，并生成基础报告草稿。

2. 项目数据分析人员  
   需要把重复的数据清洗、统计、绘图和报告初稿流程做成可演示工具。

3. 课题负责人或导师  
   需要检查工具是否能可靠生成可追溯的统计结果和报告草稿。

### 2.2 V1 不优先服务的用户

1. 多团队协作用户。
2. 需要权限管理的机构用户。
3. 需要云端部署和数据库连接的生产用户。
4. 需要自动完成科研解释或政策建议的用户。

## 3. 核心用户问题

1. 当前分析流程重复。  
   每次换站点、变量或时间范围，都要重新筛选、绘图、计算统计值和写描述。

2. 手工复制容易出错。  
   报告中的最大值、最小值、均值、标准差等数字容易被复制错、漏填或口径不一致。

3. 图表和统计口径不统一。  
   不同人员使用不同脚本，图表样式和统计定义难以保持一致。

4. AI 使用边界不清。  
   如果让 AI 直接分析原始数据或解释机理，会带来幻觉和报告可靠性风险。

5. 数据隐私要求高。  
   项目数据可能未公开。V1 必须只用合成或脱敏数据，并优先本地处理。

## 4. Jobs-to-be-Done

1. 当我拿到一份脱敏监测数据时，我想快速确认字段和数据质量，以便判断它能否进入分析。

2. 当我分析一个站点的一个变量时，我想一键生成原始时间序列图和日均时间序列图，以便快速了解变化情况。

3. 当我写报告时，我想自动得到最大值、最小值、均值、标准差和日变化幅度，以便减少手工计算和复制错误。

4. 当我使用 AI 写报告草稿时，我想让 AI 只读取计算后的统计 JSON，以便避免它编造数字或环境机制。

5. 当我向导师或项目成员演示时，我想导出图表、统计表、配置和报告草稿，以便别人可以检查结果来源。

## 5. 标准输入数据 Schema

V1 只支持一种标准长表格式。用户可上传 CSV 文件。Excel、宽表自动转换、多文件合并放到 V1.5 或未来版本。

### 5.1 必填字段

| 字段名 | 类型 | 示例 | 说明 |
|---|---|---|---|
| `timestamp` | datetime string | `2026-01-01 00:00:00` | 采样时间。必须能被 Python 或 MATLAB 解析。 |
| `station_id` | string | `ST001` | 站点编号。 |
| `station_name` | string | `A站` | 站点名称。 |
| `variable` | string | `DO` | 变量名，例如 DO、pH、COD、Chl-a。 |
| `value` | numeric | `7.82` | 监测值。必须能转为数值。 |
| `unit` | string | `mg/L` | 单位。 |

### 5.2 可选字段

| 字段名 | 类型 | 示例 | 说明 |
|---|---|---|---|
| `quality_flag` | string | `valid` | 数据质量标记。V1 只展示，不做复杂规则处理。 |
| `latitude` | numeric | `30.123` | 纬度。V1 不画地图。 |
| `longitude` | numeric | `120.123` | 经度。V1 不画地图。 |

### 5.3 输入限制

1. V1 只接受合成或脱敏 CSV。
2. V1 只分析单个上传文件。
3. V1 演示目标数据量：不超过 50,000 行。
4. V1 要求用户选择单个站点、单个变量和一个时间范围。
5. 时间列必须能解析为统一时区或无时区时间。V1 不处理跨时区问题。

## 6. 最终 V1 MVP 范围

V1 是一个本地 Streamlit 演示应用，范围严格限定如下：

1. 上传一个标准 Schema 的 CSV 文件。
2. 校验必填字段、时间格式、数值格式、缺失值和重复行。
3. 选择一个站点、一个变量和一个时间范围。
4. 用确定性 MATLAB 或 Python 逻辑计算统计结果。
5. 生成两张图：原始时间序列图、日均时间序列图。
6. 计算五类指标：最大值、最小值、均值、标准差、日变化幅度。
7. 生成结构化统计 JSON。
8. 让 AI 只基于统计 JSON 和分析配置生成中文报告草稿。
9. 导出 Markdown 报告、统计 CSV、统计 JSON 和图表 PNG。
10. 在界面和报告中明确显示 AI 使用边界。

V1 不做批量处理，不做 Excel 导入，不做异常检测图，不做地图，不做数据库连接，不做用户系统，不做云端部署。

## 7. V1.5 可选范围

以下功能只有在 V1 完成且仍有时间时才做：

1. Excel 文件导入。
2. 固定阈值异常点标记。
3. 小时均值图。
4. 多变量逐个切换分析。
5. 简单图表样式配置。
6. 一键打包下载所有导出文件。
7. 生成一个可复用的样例合成数据集。

## 8. 未来功能

以下功能不属于 2-3 周本地演示范围：

1. 多文件批量分析。
2. 多站点、多变量批量报告生成。
3. 数据库连接。
4. 地图可视化。
5. 权限管理和多用户协作。
6. 云端部署。
7. 报告版本管理。
8. 复杂异常检测模型。
9. 机器学习预测。
10. 自动生成环境机制解释、污染来源判断、政策建议或合规结论。

## 9. 非目标

1. 不处理未脱敏敏感数据。
2. 不让 AI 读取原始 CSV。
3. 不让 AI 计算任何数值结果。
4. 不让 AI 生成没有统计 JSON 支持的数字。
5. 不让 AI 编造环境机制、污染来源、因果解释或治理建议。
6. 不替代研究人员的专业判断。
7. 不承诺生成可直接发表的论文结论。
8. 不支持生产级稳定性和权限体系。

## 10. 功能需求

### 10.1 数据上传与校验

1. 用户可以上传一个 CSV 文件。
2. 系统必须校验必填字段是否存在。
3. 系统必须校验 `timestamp` 是否可解析。
4. 系统必须校验 `value` 是否可转为数值。
5. 系统必须显示总行数、站点数、变量数、时间范围。
6. 系统必须显示每个必填字段的缺失值数量和比例。
7. 系统必须显示重复行数量。
8. 若必填字段缺失、时间解析失败或数值解析失败，系统必须阻止继续分析。

### 10.2 筛选

1. 用户只能选择一个 `station_id` 或 `station_name`。
2. 用户只能选择一个 `variable`。
3. 用户可以选择开始时间和结束时间。
4. 系统必须显示筛选后的记录数。
5. 如果筛选结果为空，系统必须给出明确提示，并不生成图表或报告。

### 10.3 确定性统计计算

1. 所有数值结果必须由 MATLAB 或 Python 的确定性逻辑计算。
2. V1 默认使用 Python；如果接入 MATLAB，MATLAB 也只能作为确定性计算引擎。
3. AI 不得参与任何数值计算。
4. 必须计算：
   - `max_value`：筛选数据中的最大值。
   - `min_value`：筛选数据中的最小值。
   - `mean_value`：筛选数据中的算术平均值。
   - `std_value`：筛选数据中的样本标准差。
   - `diurnal_amplitude_mean`：先按自然日计算 `daily_max - daily_min`，再计算这些日振幅的平均值。
5. 系统必须输出每个指标的计算定义。
6. 同一输入文件和同一筛选配置重复运行，统计结果必须一致。

### 10.4 图表

1. 系统必须生成原始时间序列图。
2. 系统必须生成日均时间序列图。
3. 每张图必须包含标题、x 轴标签、y 轴标签、变量名、单位和站点信息。
4. 每张图必须能导出 PNG。
5. V1 不要求异常图。

### 10.5 结构化结果

系统必须生成统计 JSON，至少包含：

```json
{
  "input_file_name": "demo.csv",
  "station_id": "ST001",
  "station_name": "A站",
  "variable": "DO",
  "unit": "mg/L",
  "start_time": "2026-01-01 00:00:00",
  "end_time": "2026-01-07 23:00:00",
  "record_count": 168,
  "missing_value_count": 0,
  "max_value": 9.12,
  "min_value": 5.43,
  "mean_value": 7.18,
  "std_value": 0.86,
  "diurnal_amplitude_mean": 1.25,
  "calculation_engine": "Python deterministic logic"
}
```

### 10.6 AI 报告生成

1. AI 输入只能包含统计 JSON、指标定义和报告模板要求。
2. AI 不得接收原始 CSV。
3. AI 不得生成统计 JSON 之外的新数值。
4. AI 不得生成环境机制、污染来源、因果解释、治理建议或合规判断。
5. AI 输出必须包含一句边界说明：  
   “本报告草稿由 AI 基于确定性计算得到的统计结果生成，不包含未经验证的环境机制解释。”
6. 如果统计 JSON 缺少关键字段，AI 报告生成必须失败并提示用户先完成统计计算。

### 10.7 导出

1. 用户可以导出 Markdown 报告。
2. 用户可以导出统计 CSV。
3. 用户可以导出统计 JSON。
4. 用户可以导出两张 PNG 图表。
5. 导出文件名应包含站点、变量和日期范围。

## 11. 非功能需求

1. 本地运行：V1 必须能在本地电脑运行。
2. 数据隐私：V1 不上传原始数据到任何外部服务。
3. 可复现：同一输入和配置必须产生相同统计结果。
4. 可追溯：报告中的每个数字必须能在统计 JSON 中找到。
5. 性能：50,000 行以内数据，单次筛选、统计和绘图应在 10 秒内完成。
6. 易用性：新用户应能在 10 分钟内完成一次演示流程。
7. 稳定性：错误输入必须显示清楚提示，不能直接崩溃。
8. 测试性：统计计算逻辑必须能被单元测试覆盖。

## 12. 用户故事

### 用户故事 1：上传标准 CSV

作为项目数据分析人员，我想上传一份标准 Schema 的脱敏 CSV，以便系统读取环境监测数据。

验收标准：
1. 给定包含必填字段的 CSV，系统能成功读取。
2. 给定缺少 `timestamp` 的 CSV，系统阻止分析并提示缺少字段。
3. 给定 `value` 含非数值内容的 CSV，系统显示错误行数。
4. 系统显示总行数、站点数、变量数和时间范围。

### 用户故事 2：选择单站点单变量

作为研究生，我想选择一个站点、一个变量和时间范围，以便生成当前报告需要的分析结果。

验收标准：
1. 用户一次只能选择一个站点。
2. 用户一次只能选择一个变量。
3. 系统显示筛选后的记录数。
4. 空结果时，系统不生成图表、统计或报告。

### 用户故事 3：生成图表

作为报告编写人员，我想生成原始时间序列图和日均时间序列图，以便插入报告。

验收标准：
1. 系统生成原始时间序列图。
2. 系统生成日均时间序列图。
3. 图表标题包含站点名、变量名和时间范围。
4. y 轴包含变量单位。
5. 两张图都可以导出 PNG。

### 用户故事 4：计算统计指标

作为项目分析人员，我想自动计算基础统计指标，以便避免手工计算错误。

验收标准：
1. 系统计算最大值、最小值、均值、样本标准差和平均日变化幅度。
2. 所有指标由 MATLAB 或 Python 确定性逻辑计算。
3. 同一输入重复运行 3 次，结果完全一致。
4. 每个指标都有定义说明。
5. 统计结果可导出为 CSV 和 JSON。

### 用户故事 5：生成受控 AI 报告草稿

作为报告撰写人员，我想让 AI 基于统计 JSON 生成中文报告草稿，以便减少重复写作。

验收标准：
1. AI 输入不包含原始 CSV。
2. AI 文本中的所有数字都能在统计 JSON 中找到。
3. AI 文本不包含污染来源、环境机制、因果解释或治理建议。
4. AI 报告包含边界说明。
5. 缺少统计 JSON 时，系统不允许生成报告。

## 13. V1 总体验收标准

1. 使用一份合成或脱敏 CSV，可以完成上传、校验、筛选、统计、绘图、AI 报告和导出。
2. V1 演示数据不超过 50,000 行。
3. 单次筛选、统计和绘图在 10 秒内完成。
4. 统计 JSON 包含全部必填统计字段。
5. Markdown 报告中的每个数字都能在统计 JSON 中找到。
6. AI 报告中不得出现统计 JSON 之外的新数值。
7. AI 报告中不得出现环境机制、污染来源、因果解释、治理建议或合规判断。
8. 原始 CSV 不被发送给 AI。
9. 同一输入和配置重复运行 3 次，统计结果一致。
10. 缺少必填字段、时间解析失败、数值解析失败、空筛选结果都能得到明确错误提示。

## 14. 风险与假设

| 风险类别 | 风险或假设 | 影响 | V1 缓解方式 | 验证方式 |
|---|---|---|---|---|
| 数据隐私 | 用户误上传未脱敏数据 | 泄露项目敏感数据 | 页面提示只允许合成或脱敏数据；V1 不上传原始 CSV 到外部服务 | 人工检查演示数据来源 |
| 数据隐私 | AI 接收到原始数据 | 违反产品边界 | AI 输入层只传统计 JSON，不传 CSV 内容 | 审查 AI 调用 payload |
| AI 幻觉 | AI 编造新数字 | 报告不可信 | 要求数字必须来自统计 JSON | 人工抽查 20 段报告 |
| AI 幻觉 | AI 编造环境机制或污染来源 | 误导科研判断 | 提示词禁止机制解释；报告加入边界说明 | 人工审查 AI 输出 |
| 数据质量 | 时间格式混乱 | 图表和日均统计错误 | 强制时间解析校验，失败则阻止分析 | 用错误样例测试 |
| 数据质量 | 缺失值或非数值导致统计偏差 | 指标不可靠 | 显示缺失数量；非数值转换失败则阻止分析 | 用含缺失和异常格式样例测试 |
| 数据质量 | 采样频率不均匀 | 日均和日变化幅度可能偏差 | V1 报告显示记录数和日期覆盖范围，不做科学充分性判断 | 用不均匀采样数据测试 |
| 报告可靠性 | 用户把 AI 草稿当最终结论 | 产生错误传播 | 报告标注“草稿”和 AI 边界说明 | 用户测试后询问理解 |
| 计算可靠性 | MATLAB/Python 口径不一致 | 结果难复现 | V1 固定默认 Python 口径；MATLAB 仅作为可替代确定性引擎 | 用同一小数据集对照 |
| 范围风险 | 功能过多导致 2-3 周无法完成 | Demo 延期 | V1 只保留单文件、单站点、单变量、两张图、五个指标 | 每周按 V1 清单验收 |

## 15. 产品评估指标

### 15.1 V1 核心指标

| 指标 | 定义 | 目标 |
|---|---|---|
| 完整流程成功率 | 成功完成上传到导出的演示次数 / 总演示次数 | >= 80% |
| 统计复现率 | 同一输入重复运行结果一致的次数 / 重复运行次数 | 100% |
| AI 越界率 | AI 输出出现新增数值或机制解释的次数 / AI 生成次数 | 0 |
| 图表导出成功率 | 成功导出 PNG 的次数 / 尝试导出次数 | >= 95% |
| 单次分析耗时 | 50,000 行以内完成筛选、统计、绘图的时间 | <= 10 秒 |

### 15.2 V1 用户反馈指标

| 指标 | 定义 | 目标 |
|---|---|---|
| 上手时间 | 新用户完成首次演示流程所需时间 | <= 10 分钟 |
| 数字可信度评分 | 用户对统计结果可追溯性的 1-5 分评分 | >= 4 |
| 报告草稿可用性评分 | 用户对 AI 草稿可编辑性的 1-5 分评分 | >= 3.5 |
| 错误提示清晰度 | 用户对错误提示的 1-5 分评分 | >= 4 |

## 16. 发布计划

### 16.1 V1：2-3 周本地 Demo

目标是做出一个可信的本地演示闭环，而不是生产系统。

交付内容：
1. 本地 Streamlit 应用。
2. 标准 CSV Schema。
3. 一份合成演示数据。
4. 数据校验、筛选、统计、两张图。
5. 统计 JSON。
6. 受控 AI 中文报告草稿。
7. Markdown、CSV、JSON、PNG 导出。

### 16.2 V1.5

目标是在 V1 稳定后扩展演示能力。

候选内容：
1. Excel 导入。
2. 异常点标记。
3. 小时均值。
4. 简单批量切换。
5. 打包下载。

### 16.3 未来版本

目标是在真实项目验证后，再考虑生产化能力。

候选内容：
1. 批量报告。
2. 数据库连接。
3. 团队协作。
4. 权限管理。
5. 云端部署。
6. 高级统计和模型，但仍必须由确定性代码执行。

## 17. V1 决策原则

1. 能不做的先不做。
2. 先保证数字可信，再提高自动化程度。
3. AI 只写文字，不算数，不解释机制。
4. 每个报告数字都必须来自统计 JSON。
5. 本地演示优先，生产化以后再说。

# CURRENT_STATUS

## 当前状态：V4.2 旧月际算法为稳定正式实现（2026-07-18）

- V4.2 正式月际算法继续作为当前稳定实现，后续正式开发基于 `feature/v4.2-stable-continuation`。
- 月际质量评估已完成：27 个样本中未发现主要趋势或核心事实重大错误；主要不足是部分文字过度拆分。
- 月际叙述压缩实验保留在 `feature/monthly-narrative-compression`，暂不合并到正式路线。
- 报告导出前继续保留人工快速审阅；评估工具可用于复核，但不改变正式算法。

## 当前状态：月际报告质量评估 baseline 已完成，等待冻结

- 基线：V4.2 冻结提交 `abdff0bc249a4707949515ab614afc707be4fcd9`，标签 `v4.2-station-report-complete`。
- 当前分支：`feature/report-quality-evaluation`；本阶段未修改月际解释算法。
- 待冻结候选：通用评估数据集扫描、盲标/基线导出 CLI、合成数据测试、流程说明和私有评估输入/输出忽略规则。
- 评估覆盖当前 27 个真实样本（北海、威海黄海靖、西霞口；九变量）：development 18 个、holdout 9 个。人工标注用于建立参考标准；holdout 不得用于规则调参。
- 已完成 V4.2 旧算法 baseline 评价：未发现主要趋势或核心阶段的重大错误；主要问题是部分月际文字过度拆分。当前算法适合作为内部报告初稿生成器；下一步仅做阶段压缩和叙述合并。
- 本阶段不修改统计值、关键月份、质控流程或 Word 结构；私有评价数据、盲图、评价表和 baseline 输出不进入 Git。

## 历史状态：V4.2 冻结前记录

## 当前状态：V4.2 可冻结基线

V4.2 站点任务与综合报告版本已经完成，正在执行验收、文档同步和 Git 冻结。

当前有效能力：

- 一个站点任务对应九个固定变量文件；
- 九变量均由用户手动上传 Excel；
- 未上传变量显示“未提供”，正式流程不读取 `data_private/` 默认文件；
- 每个变量沿用现有自动质控、Hampel 候选、恒定值候选、人工删除/保留/恢复和最终确认流程；
- 所有候选记录必须明确删除或保留后，才能确认最终质控结果；
- 正式统计、绘图、Excel 和 Word 报告仅使用有效确认后的 `final_qc_data`；
- 站点综合 Excel 和站点综合 Word 均采用严格九变量确认门槛；
- 单变量 Word 报告继续保留，并继承全局站点信息；
- 站点综合 Word 包含站点数据与质控汇总、两张多要素日平均总览图和九个监测要素详细章节；
- 报告解读继续采用本地确定性规则，不接入大模型完成核心事实判断；
- 当前版本不进行调和分析。

## 当前 Git 状态

- 冻结前基线提交：`09b7598`
- 冻结前基线标签：`v4.1.1-single-variable-report-complete`
- 当前分支：`feature/v3-general-variables`
- V4.2 修改尚待创建冻结提交与标签。

## 测试状态

- 全量测试命令：`python -m pytest tests -q -p no:cacheprovider`
- 冻结前测试结果：`23 passed`
- 覆盖范围：
  - 九变量站点任务状态与严格导出门槛；
  - 未上传变量不读取默认文件；
  - 报告缓存失效；
  - 站点综合 Word 上下文与结构；
  - 单变量 Word 报告回归；
  - 时间序列解释回归。

## 待冻结事项

- 创建 V4.2 冻结提交；
- 创建 annotated tag：`v4.2-station-report-complete`；
- 从冻结标签创建后续开发分支：`feature/report-quality-evaluation`。

## 下一阶段

下一阶段为报告解读质量评估。该阶段不应改变既有质控规则、统计口径、九变量固定事实或 `final_qc_data` 作为正式数据源的原则。

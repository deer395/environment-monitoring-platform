# V4.1.1 时间变化正文

- 三段综合分析须按小时、日内、月平均顺序且全部位于三图之前。
- 小时极值、主体范围、最大日变化和月平均极值全文只出现一次；真实 PAHs、水深、温度、盐度和 BOD 报告均须通过文本断言。

# V4.1.1 模式驱动叙述

- 月平均多阶段识别与周期识别需要有独立合成测试。
- 事实按小时、日内、月平均顺序渲染，避免重复和内部标签泄漏。

# V4.1.1 差异化叙述

- Word 正文不显示内部枚举；相同小时极值、主体范围和月际事实仅渲染一次。
- 报告使用转折、回升、回落、阶段稳定和周期波动等本地确定性表达；不依赖大模型。

# V4.1.1 自然语言

- 月平均阶段文本应能覆盖完整观测期，不只取首段。
- 报告自然段不得使用字段式分号拼接，不得出现 NaN 或未说明层级的极值。

# V4.1.1

- 报告数据摘要不展示质控删除或重复时间内容。
- 报告统计表展示 P5、P95 和日变化幅度 P95；叙述仅使用本地确定性规则。
- 已完成真实 PAHs、生化需氧量、水深、盐度和温度报告抽查，正文与图件未见明显冲突。
- 单变量 Word 自动报告已达到项目展示和后续开发基础要求。

# V4.1

- 未人工确认或确认 token 与当前数据源、时间范围、QC 参数不一致时，禁止生成报告。
- 单变量报告仅使用确认后的 final_qc_data 及其现有 QC、重采样、距平和统计结果，不重新运行 QC。
- 报告包含封面、数据与质控概况、质控处理、原始与最终质控对比图、基础统计、三类时间图、结论和附录。
- Hampel 与恒定值在报告中仅作为候选标记；传感器零值与 hard_range 为自动剔除。

# 验收标准

## V2

- hard_range 自动处理且不可恢复；
- Hampel 与 constant_value 默认只标记；
- 人工删除、保留和恢复可追溯到 QC 日志；
- 后续重采样、统计、绘图和导出只使用 final_qc_data；
- 原始 Excel 文件不被修改。

## V3

- 新增普通变量主要通过 `src/variable_registry.py` 和别名配置完成；
- app.py、metrics.py、loaders.py 不按变量名称复制分析主流程；
- 所有已支持变量执行小时平均、日平均、日内距平、日变化幅度、月统计、图表和导出；
- session_state 按 variable_key 隔离；
- depth 和 temperature 保持回归通过。

## V3.4

- app.py 中每个运行时函数和常量只保留一个有效定义；
- 九个已支持变量均可完成 Excel 读取、hard_range、候选标记、人工复核、final_qc_data、统计、图件和表格导出；
- 综合工作簿逐变量生成并包含 processing_status，缺失或空变量不阻断其他变量导出；
- 导出明确区分人工确认和仅自动质控；
- PAHs 单位为项目确认的 `ppb`；
- 所有 Python 文件通过 AST 语法解析，V2、V3.1、V3.2、V3.3 和 V3.4 回归脚本通过。

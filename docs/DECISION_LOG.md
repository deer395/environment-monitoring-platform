# DECISION_LOG

## 使用说明

* 出现以下情况时，应新增决策记录：

  * 项目目标、范围或使用场景发生实质变化；
  * 数据流程、质控规则、统计口径或报告结构发生变化；
  * 多个可选方案经过比较后形成明确结论；
  * 某项方案会持续影响后续代码、测试、文档或项目展示；
  * 原有方案被明确推翻、替代或停止使用。

* 以下内容只应更新 `CURRENT_STATUS.md`，不应新增决策：

  * 当前分支、提交哈希和标签；
  * 一次测试结果；
  * 当前报错或临时缺陷；
  * 最近完成的任务；
  * 某个文件是否已修改；
  * 尚未形成长期影响的一次性操作；
  * 下一步执行顺序发生的小幅调整。

* 决策被推翻时：

  * 不删除原决策；
  * 将原决策状态改为“已替代”或“已否决”；
  * 在原决策中注明替代它的新决策编号；
  * 在新决策中注明其替代的旧决策编号；
  * 保留原决策的背景、理由和问题表现。

* 不得删除仍有解释价值的历史决策。已经失败的方案如果能够说明问题来源、项目迭代逻辑或技术取舍，应继续保留，供后续维护和面试复盘使用。

## 决策记录

### D-001 项目以求职展示价值为当前主要边界

* 状态：暂定采用
* 决策时间：本长对话阶段内，准确日期待确认
* 背景：

  * 项目来源于环境监测数据的重复处理需求；
  * 项目已经具有真实数据处理和报告生成能力；
  * 用户明确表示，当前主要目的是形成一段有说服力的项目经历，而不是立即开发完整商业产品。
* 需要解决的问题：

  * 是否继续追求商业级完整度；
  * 应在什么完成度停止扩展和打磨。
* 可选方案：

  * 按商业级产品标准持续完善；
  * 以求职展示为主要目标，同时保留实际工具属性；
  * 仅制作演示原型，不保留真实处理能力。
* 最终采用方案：

  * 当前以求职展示价值为主要边界，同时保留真实环境监测数据处理工具属性。
* 采用理由：

  * 当前项目已经能够展示数据处理、质控、人工复核、统计、绘图、报告、测试和版本管理的完整流程；
  * 继续追求所有文本达到人工专家水平的投入产出比较低；
  * 用户不要求当前版本达到商业级或行业正式交付级完整度。
* 未采用方案及原因：

  * 商业级持续扩展：当前投入与求职收益不匹配；
  * 仅做演示原型：会失去项目已有的真实业务处理价值。
* 依据：

  * 用户明确说明项目主要用于形成求职经历；
  * 最新四个随机变量报告达到无明显图文冲突的可接受状态后，用户同意收尾。
* 影响范围：

  * 功能范围控制；
  * 报告语言优化深度；
  * 测试和部署要求；
  * 后续开发优先级。
* 风险与代价：

  * 当前工具不能被描述为商业级或行业认证产品；
  * 部分语言、格式和边缘场景仍可能不够精细。
* 可否逆转：

  * 可以。
* 重新评估条件：

  * 项目需要正式交付；
  * 课题组决定长期使用；
  * 用户决定将项目发展为正式产品。
* 与其他决策的关系：

  * 与 D-014、D-015 和 D-016 直接相关；
  * 下一阶段最终定位仍见 D-019。

### D-002 核心分析和报告采用本地确定性规则

* 状态：已确认
* 决策时间：本长对话阶段内，准确日期待确认
* 背景：

  * 曾讨论过使用大模型 API 对报告进行生成或润色；
  * 当前报告曾出现方向识别和阶段判断错误。
* 需要解决的问题：

  * 报告事实应由本地算法还是大模型生成；
  * 如何保证数字、月份、方向和结论可复现。
* 可选方案：

  * 完全由大模型生成报告；
  * 本地算法生成事实，大模型负责润色；
  * 完全采用本地确定性规则；
  * 当前使用本地规则，未来再讨论可选润色层。
* 最终采用方案：

  * 核心数据计算、事实提取和单变量报告使用本地确定性规则；
  * 当前不接入大模型 API。
* 采用理由：

  * 如果结构化事实错误，大模型只会将错误表达得更自然；
  * 本地规则更容易测试、复现和追溯；
  * API 会增加密钥、网络、成本、隐私、失败降级和数字保护等额外复杂度；
  * 当前求职项目不需要依赖大模型才能成立。
* 未采用方案及原因：

  * 完全由大模型生成：无法保证数字和事实稳定；
  * 当前立即增加润色层：会扩大范围，但不能解决底层识别错误。
* 依据：

  * 多轮报告检查表明，主要问题来自阶段识别而非单纯措辞；
  * 用户确认当前交接文件中不保留大模型润色作为既定路线。
* 影响范围：

  * 报告架构；
  * 数据安全；
  * 测试方式；
  * 部署复杂度；
  * 项目面试表述。
* 风险与代价：

  * 本地文本可能具有一定模板感；
  * 复杂语言表达能力受规则范围限制。
* 可否逆转：

  * 可以。
* 重新评估条件：

  * 用户未来明确提出需要语言润色；
  * 本地事实层已经稳定；
  * 能建立数字保护、失败降级和可关闭机制。
* 与其他决策的关系：

  * 与 D-006、D-010、D-012 和 D-013 共同构成确定性报告框架。

### D-003 自动质控与人工复核结合，最终分析统一使用 final_qc_data

* 状态：已确认
* 决策时间：本长对话阶段内，准确日期待确认
* 背景：

  * 自动异常规则可能产生误报；
  * 完全人工处理效率低；
  * 项目需要保留质控过程的可追溯性。
* 需要解决的问题：

  * 哪些记录由系统自动处理；
  * 哪些记录需要人工决定；
  * 后续统计使用哪个数据版本。
* 可选方案：

  * 所有候选异常自动删除；
  * 只提供人工清洗；
  * 自动处理确定性无效值，候选异常交由人工复核；
  * 统计直接使用自动质控结果。
* 最终采用方案：

  * 传感器零值和硬范围外数据由自动规则处理；
  * Hampel 和连续恒定值只生成候选；
  * 人工决定候选点和普通点的保留、删除或恢复；
  * 最终统计、绘图和报告统一使用 `final_qc_data`；
  * 全过程写入最终质控日志。
* 采用理由：

  * 自动规则提高效率；
  * 人工复核降低误删风险；
  * `final_qc_data` 能保证所有后续结果使用同一数据基线；
  * 日志能够支持追溯。
* 未采用方案及原因：

  * 候选异常全部自动删除：误删风险较高；
  * 完全人工清洗：效率低；
  * 直接使用自动质控结果：无法反映人工确认。
* 依据：

  * 项目现有流程和报告结构；
  * 用户对人工确认后再进入计算的要求。
* 影响范围：

  * 质控模块；
  * 人工复核界面；
  * 重采样；
  * 统计；
  * 图件；
  * Word 和 Excel 输出；
  * 日志。
* 风险与代价：

  * 人工复核需要额外操作；
  * 结果会受到人工判断影响；
  * 参数变化后需要重新确认。
* 可否逆转：

  * 可以，但属于核心架构变更。
* 重新评估条件：

  * 出现可靠性更高且经过验证的全自动规则；
  * 实际使用场景不再允许人工参与；
  * 质控业务规则发生变化。
* 与其他决策的关系：

  * 是所有报告和统计决策的前置条件；
  * 与 D-002 的可复现性要求一致。

### D-004 固定支持九个变量并通过变量注册表统一管理

* 状态：已确认
* 决策时间：本长对话阶段内，准确日期待确认
* 背景：

  * 早期代码中存在变量名称、单位和参数硬编码；
  * 项目需要支持多个变量使用同一流程。
* 需要解决的问题：

  * 当前需要支持哪些变量；
  * 变量名称、单位、范围和质控参数应在哪里维护。
* 可选方案：

  * 只支持少量核心变量；
  * 每个变量单独维护代码；
  * 固定九个变量，并通过变量注册表集中管理；
  * 构建完全动态、无限制的变量系统。
* 最终采用方案：

  * 固定启用九个变量，不再删减：

    1. 水深；
    2. 温度；
    3. 盐度；
    4. 溶解氧；
    5. 化学需氧量；
    6. 生化需氧量；
    7. 硝酸盐；
    8. 叶绿素；
    9. 多环芳烃。
  * 变量名称、单位、范围和质控参数通过变量注册表集中管理。
* 采用理由：

  * 九个变量覆盖当前真实项目需要；
  * 集中注册可以减少页面、图件、表格和报告之间的不一致；
  * 不需要为求职项目继续扩展无限变量系统。
* 未采用方案及原因：

  * 继续删减变量：用户明确确认九个变量均保留；
  * 每个变量独立实现：会造成重复和硬编码；
  * 无限动态变量系统：当前没有必要。
* 依据：

  * 用户明确确认变量固定为九个，不再删减；
  * 项目已有变量注册表结构。
* 影响范围：

  * 页面选择；
  * 数据加载；
  * 质控参数；
  * 图件；
  * Excel；
  * Word；
  * 测试。
* 风险与代价：

  * 新增第十个变量仍需要注册配置；
  * 不同设备或站点可能需要重新核验参数。
* 可否逆转：

  * 可以。
* 重新评估条件：

  * 出现明确的新变量需求；
  * 某变量被业务正式取消；
  * 项目转为更通用的平台。
* 与其他决策的关系：

  * 与 D-013 的禁止变量专用分支共同保证通用性。

### D-005 温度单位全链路统一为 ℃

* 状态：已确认
* 决策时间：本长对话阶段内，准确日期待确认
* 背景：

  * 温度曾使用 `degC`；
  * 报告封面还出现过“温度（ ）℃”的格式问题。
* 需要解决的问题：

  * 温度在页面、图件、Word、Excel 和 Summary 中应使用什么单位；
  * 如何避免不同输出位置不一致。
* 可选方案：

  * 保留 `degC`；
  * 仅在 Word 中改为 `℃`；
  * 从变量注册表统一改为 `℃`。
* 最终采用方案：

  * 温度用户可见单位全链路统一为 `℃`。
* 采用理由：

  * 用户明确要求使用 `℃`；
  * 集中修改可以避免不同模块单位不一致。
* 未采用方案及原因：

  * 保留 `degC`：不符合用户要求；
  * 仅修改 Word：会造成页面、图件和导出不一致。
* 依据：

  * 用户确认页面、图件、Word、Excel 和 Summary 均已统一为 `℃`。
* 影响范围：

  * 变量注册表；
  * 页面；
  * 图轴；
  * 表格；
  * Word；
  * Excel；
  * Summary。
* 风险与代价：

  * 数据加载不能依赖显示单位字符串；
  * 后续修改变量单位时需要同步核验全链路。
* 可否逆转：

  * 可以，但没有当前需求。
* 重新评估条件：

  * 数据源或行业输出规范明确要求其他温度单位。
* 与其他决策的关系：

  * 是 D-004 集中元数据管理的具体应用。

### D-006 固定模板加少量模式句子的报告方案

* 状态：已替代
* 决策时间：本长对话阶段内，准确日期待确认
* 背景：

  * 早期报告使用相似的基础模板，只根据少量模式增加不同句子。
* 需要解决的问题：

  * 如何快速生成不同变量的自动报告。
* 可选方案：

  * 固定模板；
  * 固定模板加少量模式句；
  * 按小时、日内和月际分别提取事实并生成内容。
* 最终采用方案：

  * 该方案曾作为早期实现方式，后被 D-007 替代。
* 采用理由：

  * 初期实现简单；
  * 能快速建立自动报告闭环。
* 未采用方案及原因：

  * 后续不再采用，因为不同变量描述过于相似，月际过程也容易被简化。
* 依据：

  * 多份报告检查发现内容雷同，且阶段路径表达不足。
* 影响范围：

  * 报告文本；
  * 时间序列解释模块；
  * 结论生成。
* 风险与代价：

  * 报告模板感明显；
  * 容易遗漏变量真正重要的特征。
* 可否逆转：

  * 技术上可以，但不应恢复为当前主方案。
* 重新评估条件：

  * 仅生成极简摘要且不需要阶段解释。
* 与其他决策的关系：

  * 被 D-007 替代；
  * 与 D-010、D-012 的形成有关。

### D-007 报告内容按 hourly、intraday、monthly 三个尺度组织

* 状态：已确认
* 决策时间：本长对话阶段内，准确日期待确认
* 背景：

  * 固定模板难以区分不同时间尺度的事实；
  * 小时极值、日内周期和月际变化容易混在同一段。
* 需要解决的问题：

  * 报告事实如何分层；
  * 不同尺度的统计信息如何避免重复。
* 可选方案：

  * 全部事实写在一个综合段落；
  * 按图件逐段说明；
  * 按 `hourly → intraday → monthly` 三个尺度独立生成。
* 最终采用方案：

  * 时间变化综合分析固定分为：

    * `hourly`；
    * `intraday`；
    * `monthly`。
  * 各尺度只描述自身负责的事实。
* 采用理由：

  * 能够明确区分主体范围、周期、日变化和月际阶段；
  * 结构统一；
  * 便于测试和维护；
  * 不需要通过随机句式制造差异。
* 未采用方案及原因：

  * 单一综合段落：事实容易混杂；
  * 随机变化句式：不能提高事实准确性。
* 依据：

  * 用户明确要求综合分析采用该顺序；
  * 最新报告均已采用。
* 影响范围：

  * 报告上下文；
  * 叙述生成；
  * Word 排版；
  * 测试。
* 风险与代价：

  * 某些变量在部分尺度上特征不明显，文字可能较通用；
  * 需要控制不同段落之间的重复。
* 可否逆转：

  * 可以，但属于报告长期结构变更。
* 重新评估条件：

  * 报告使用场景要求另一种正式模板。
* 与其他决策的关系：

  * 替代 D-006；
  * 与 D-009、D-010 和 D-012 配套。

### D-008 图文交替排版方案

* 状态：已否决
* 决策时间：本长对话阶段内，准确日期待确认
* 背景：

  * 曾考虑采用“图 2 后一段、图 3 后一段、图 4 后一段”的结构。
* 需要解决的问题：

  * 分析文字和三张时间变化图如何排列。
* 可选方案：

  * 每张图后紧跟对应分析；
  * 先连续放图，再写综合分析；
  * 先写三尺度综合分析，再连续展示三张图。
* 最终采用方案：

  * 图文交替方案被用户明确否决；
  * 由 D-009 的先文后图方案替代。
* 采用理由：

  * 该方案曾被视为一种可能的图文对应方式。
* 未采用方案及原因：

  * 不符合用户最终要求；
  * 会造成报告阅读结构与用户预期不一致。
* 依据：

  * 用户明确要求不是“一张图一段文字”。
* 影响范围：

  * Word 报告排版。
* 风险与代价：

  * 若恢复该方案，会破坏当前固定结构。
* 可否逆转：

  * 可以，但必须由用户明确重新决定。
* 重新评估条件：

  * 用户提供新的正式报告模板。
* 与其他决策的关系：

  * 被 D-009 替代。

### D-009 时间变化综合分析整体位于图 2、图 3、图 4 之前

* 状态：已确认
* 决策时间：本长对话阶段内，准确日期待确认
* 背景：

  * 图文交替和先图后文均不符合用户最终要求。
* 需要解决的问题：

  * 三段分析文字与三张时间变化图的最终排版顺序。
* 可选方案：

  * 图文交替；
  * 先图后文；
  * 先写完整综合分析，再连续放置三张图。
* 最终采用方案：

  * 先写 `hourly → intraday → monthly` 三段综合分析；
  * 随后连续展示图 2、图 3、图 4。
* 采用理由：

  * 符合用户明确要求；
  * 保持分析逻辑连续；
  * 避免在文字和图件之间反复跳转。
* 未采用方案及原因：

  * 图文交替：已由用户否决；
  * 先图后文：与最终阅读顺序不符。
* 依据：

  * 用户明确指定该排版；
  * 最新四份抽查报告采用该结构。
* 影响范围：

  * Word 报告结构；
  * 图题和分页。
* 风险与代价：

  * 分析文字与具体图件之间存在一定页面距离。
* 可否逆转：

  * 可以，但必须修改长期报告结构。
* 重新评估条件：

  * 出现新的正式模板要求。
* 与其他决策的关系：

  * 替代 D-008；
  * 与 D-007 共同确定报告结构。

### D-010 只寻找单一最大月际跳变的算法

* 状态：已替代
* 决策时间：本长对话阶段内，准确日期待确认
* 背景：

  * 早期月平均解释主要依据最大相邻变化，将序列划分为少量前后阶段。
* 需要解决的问题：

  * 如何从月平均序列中识别主要变化。
* 可选方案：

  * 只找最大跳变；
  * 只判断总体上升或下降；
  * 识别多阶段方向和反转。
* 最终采用方案：

  * 单一最大跳变方案曾用于早期实现，后被 D-011 替代。
* 采用理由：

  * 实现简单；
  * 能快速识别明显水平变化。
* 未采用方案及原因：

  * 无法正确处理先降后升、先升后降和多次阶段变化；
  * 曾导致温度回升仍被描述为持续下降；
  * 曾出现单月 `X～X` 波动和方向错误。
* 依据：

  * 多份真实报告图文检查。
* 影响范围：

  * 月平均阶段；
  * 正文；
  * 结论。
* 风险与代价：

  * 对复杂序列过度简化；
  * 容易遗漏后期反转。
* 可否逆转：

  * 不应恢复为当前主算法。
* 重新评估条件：

  * 仅处理明确单一变点的特殊数据。
* 与其他决策的关系：

  * 被 D-011 替代；
  * 导致 D-012 的结论策略也需要调整。

### D-011 月平均采用通用多阶段方向识别

* 状态：已确认
* 决策时间：本长对话阶段内，准确日期待确认
* 背景：

  * 单一最大跳变不能准确描述真实月平均过程。
* 需要解决的问题：

  * 如何通用识别上升、下降、稳定、回升和回落；
  * 如何避免针对单个变量修补文字。
* 可选方案：

  * 继续使用单一变点；
  * 为各变量编写专用规则；
  * 使用通用多阶段方向识别。
* 最终采用方案：

  * 基于完整月平均序列识别并合并：

    * 持续上升；
    * 持续下降；
    * 相对稳定；
    * 阶段性回升；
    * 阶段性回落；
    * 短暂回升；
    * 短暂回落。
  * 连续同方向月份合并；
  * 单月不写成区间波动；
  * “进一步”必须满足方向持续性；
  * 方向明确时不使用“变化至”。
* 采用理由：

  * 能覆盖不同变量的多阶段过程；
  * 能减少图文方向冲突；
  * 不依赖变量名称；
  * 最新随机抽查结果达到可接受状态。
* 未采用方案及原因：

  * 单一变点：无法覆盖复杂路径；
  * 变量专用规则：会破坏通用性并形成过拟合。
* 依据：

  * 温度、水深、化学需氧量、生化需氧量、盐度、多环芳烃等报告的多轮检查；
  * 最新四份随机报告未发现明显方向错误。
* 影响范围：

  * 时间序列解释；
  * 报告上下文；
  * Word 正文；
  * 结论；
  * 测试。
* 风险与代价：

  * 算法复杂度增加；
  * 某些复杂序列仍可能产生较机械或较细的分段。
* 可否逆转：

  * 可以，但属于核心报告算法变更。
* 重新评估条件：

  * 新数据出现稳定复现的明显误判；
  * 使用场景需要更严格的统计分段方法。
* 与其他决策的关系：

  * 替代 D-010；
  * 受 D-013 的通用算法约束；
  * 为 D-012 提供事实基础。

### D-012 结论基于主要全过程并优先保留强周期特征

* 状态：已确认
* 决策时间：本长对话阶段内，准确日期待确认
* 背景：

  * 早期结论可能只复述月平均第一阶段；
  * 或固定列出最高月、最低月和标准差最大月；
  * 水深结论曾遗漏约 24 小时周期。
* 需要解决的问题：

  * 如何保证结论覆盖整个观测期；
  * 如何选择最重要的统计事实。
* 可选方案：

  * 复制月平均第一句；
  * 固定输出高值、低值和最大波动月；
  * 根据主要变化路径、周期、转折和波动选择重点。
* 最终采用方案：

  * 结论从以下内容中选择最有代表性的两至三项：

    * 主要月际路径；
    * 高值或低值阶段；
    * 明显回升或回落；
    * 强周期；
    * 波动较大的月份。
  * 强周期变量的结论优先包含周期。
* 采用理由：

  * 能覆盖完整观测过程；
  * 避免正文和结论冲突；
  * 水深等变量的核心特征不再被遗漏。
* 未采用方案及原因：

  * 复制第一阶段：会遗漏后期变化；
  * 固定极值拼接：可能忽略周期和主要路径。
* 依据：

  * 早期盐度、叶绿素和水深报告问题；
  * 最新水深报告已经正确包含约 24 小时周期。
* 影响范围：

  * 报告结论；
  * 时间序列事实优先级；
  * 测试。
* 风险与代价：

  * 需要定义事实优先级；
  * 不同变量结论仍可能保持统一句式。
* 可否逆转：

  * 可以。
* 重新评估条件：

  * 报告使用方要求固定结论模板；
  * 出现主要事实选择不合理的重复案例。
* 与其他决策的关系：

  * 依赖 D-007 和 D-011；
  * 与 D-002 的确定性报告原则一致。

### D-013 通用算法不得通过变量名称专用分支修复

* 状态：已确认
* 决策时间：本长对话阶段内，准确日期待确认
* 背景：

  * 多个真实变量在不同阶段出现不同描述问题；
  * 逐变量修补可以快速通过样例，但会导致过拟合。
* 需要解决的问题：

  * 修复报告错误时，是写变量专用条件，还是修改通用事实算法。
* 可选方案：

  * 按变量名称增加专用 `if` 分支；
  * 按具体月份和数值硬编码；
  * 修改通用阶段、持续性和方向判断。
* 最终采用方案：

  * 不允许通过变量名称、固定月份或具体数值专用分支修复报告；
  * 必须修改通用算法并结合合成测试和真实报告验证。
* 采用理由：

  * 项目目标是通用多变量工具；
  * 变量专用修补无法推广到其他区域或数据；
  * 容易掩盖底层算法问题。
* 未采用方案及原因：

  * 变量专用分支：维护成本高，且容易过拟合；
  * 固定文本：无法适应数据变化。
* 依据：

  * 用户明确要求算法套入不同变量后均与图件大体一致；
  * 多次 Codex 指令均将“禁止变量专用分支”列为条件。
* 影响范围：

  * 时间序列算法；
  * 测试设计；
  * 代码审查；
  * 后续新增变量。
* 风险与代价：

  * 通用算法修复通常比单变量修补更复杂；
  * 个别变量的语言可能不如专用模板精细。
* 可否逆转：

  * 原则上可以，但会改变项目通用定位。
* 重新评估条件：

  * 业务正式要求某变量采用独立行业规则，且该规则有明确依据。
* 与其他决策的关系：

  * 与 D-004 和 D-011 共同保证多变量通用性。

### D-014 不再扩展复杂模式体系，控制单变量报告范围

* 状态：已确认
* 决策时间：本长对话阶段内，准确日期待确认
* 背景：

  * 报告优化一度向更复杂的时间序列分类和多模式系统扩展；
  * 用户当前主要需要求职项目经历。
* 需要解决的问题：

  * 是否继续增加模式、分类和语言生成复杂度；
  * 何时停止单变量报告算法打磨。
* 可选方案：

  * 继续扩展更多分析模式；
  * 重构为更复杂的时间序列解释系统；
  * 保留现有模式框架，只修复明显错误后停止。
* 最终采用方案：

  * 保留当前模式框架；
  * 不再扩大六类模式体系；
  * 只处理明显方向、图文冲突和结论遗漏；
  * 达到无明显错误后停止单变量算法优化。
* 采用理由：

  * 继续扩展对求职展示的边际收益较低；
  * 当前核心流程已经完整；
  * 用户不要求商业级或专家级自动解释。
* 未采用方案及原因：

  * 继续扩展模式：容易造成过度开发；
  * 完整重构解释系统：超出当前项目边界。
* 依据：

  * 用户明确提出项目主要为了形成经历；
  * 最新四个随机变量报告无明显问题后，双方确认可以收尾。
* 影响范围：

  * 功能范围；
  * 代码复杂度；
  * 测试投入；
  * 后续路线。
* 风险与代价：

  * 边缘序列仍可能产生不够自然的描述；
  * 当前模式体系不是完整的时间序列分析框架。
* 可否逆转：

  * 可以。
* 重新评估条件：

  * 出现正式交付需求；
  * 大量新数据暴露系统性误判；
  * 用户明确将自动解释作为核心产品能力。
* 与其他决策的关系：

  * 受 D-001 的项目边界约束；
  * 直接导致 D-015 的版本冻结。

### D-015 V4.1.1 作为单变量确定性报告完成基线冻结保存

* 状态：已确认
* 决策时间：本长对话阶段内，准确日期待确认
* 背景：

  * 单变量报告经过多轮修复和真实变量抽查；
  * 剩余问题主要是格式和语言精细度。
* 需要解决的问题：

  * 当前版本是否继续修改；
  * 是否达到可提交、打标签和迁移对话的条件。
* 可选方案：

  * 继续随机抽取变量并调算法；
  * 仅修复明显错误后冻结；
  * 放弃当前报告功能。
* 最终采用方案：

  * 将 V4.1.1 作为已完成的单变量自动报告基线；
  * 不再继续重构核心算法；
  * 完成提交和标签保存。
* 采用理由：

  * 最新随机抽查的多环芳烃、生化需氧量、水深和盐度报告未发现明显图文冲突；
  * 核心功能已满足求职展示；
  * 继续优化主要提升语言精细度。
* 未采用方案及原因：

  * 持续随机抽查和调参：投入产出比低；
  * 放弃报告功能：没有必要，当前功能已经可用。
* 依据：

  * 用户确认 Codex 保存结果：

    * 提交哈希 `09b7598`；
    * 标签 `v4.1.1-single-variable-report-complete`；
    * 工作区干净；
    * 轻量测试 `10 passed`；
    * V3.4 回归验证通过。
* 影响范围：

  * Git 基线；
  * 项目文档；
  * 新对话交接；
  * 后续开发范围。
* 风险与代价：

  * 当前版本仍保留少量非阻断格式和语言问题；
  * 不是九个变量的完整商业级验收。
* 可否逆转：

  * 可以通过后续新版本继续修改；
  * 不应修改既有标签所代表的历史基线。
* 重新评估条件：

  * 发现明显数值错误、图文冲突或回归问题；
  * 下一阶段必须修改报告公共模块。
* 与其他决策的关系：

  * 基于 D-011、D-012、D-013 和 D-014；
  * 低优先级遗留项按 D-016 处理。

### D-016 不为低优先级格式问题重新打开 V4.1.1

* 状态：已确认
* 决策时间：本长对话阶段内，准确日期待确认
* 背景：

  * 报告仍存在千位分隔和百分比小数位不完全统一的问题。
* 需要解决的问题：

  * 是否为了格式统一重新修改、提交和打标签。
* 可选方案：

  * 立即重新打开 V4.1.1；
  * 单独创建格式修复提交；
  * 在下一次正常修改报告模块时一并处理。
* 最终采用方案：

  * 不单独重新打开 V4.1.1；
  * 千位分隔和百分比格式在下一次修改报告模块时统一。
* 采用理由：

  * 这些问题不影响统计正确性、质控逻辑、图文一致性和求职展示；
  * 单独重新提交的收益较低。
* 未采用方案及原因：

  * 立即修复：会打断已经完成的版本保存；
  * 单独提交：当前必要性不足。
* 依据：

  * 用户询问是否需要重新修改；
  * 双方确认可以延后到下一阶段处理。
* 影响范围：

  * Word 文本格式；
  * 表格格式；
  * 后续报告模块维护。
* 风险与代价：

  * 当前版本中格式仍可能不完全统一。
* 可否逆转：

  * 可以。
* 重新评估条件：

  * 当前报告将直接用于正式交付；
  * 下一阶段修改相关报告代码；
  * 格式问题影响展示。
* 与其他决策的关系：

  * 建立在 D-015 已冻结基线之上。

### D-017 使用 PROJECT_MASTER、CURRENT_STATUS 和 DECISION_LOG 分离交接信息

* 状态：已确认
* 决策时间：本长对话阶段内，准确日期待确认
* 背景：

  * 原对话内容较长；
  * 需要迁移到同一 ChatGPT 项目下的新对话；
  * 长期事实、当前状态和历史决策容易混在一起。
* 需要解决的问题：

  * 如何让新对话直接接手；
  * 如何避免每次重新回顾全部长对话。
* 可选方案：

  * 继续依赖原长对话；
  * 只维护一个综合说明文件；
  * 分别维护长期主文档、当前状态和决策日志。
* 最终采用方案：

  * `PROJECT_MASTER.md` 保存长期稳定信息；
  * `CURRENT_STATUS.md` 保存当前阶段和易过期状态；
  * `DECISION_LOG.md` 保存实质性决策、替代关系和失败教训。
* 采用理由：

  * 能减少新对话上下文；
  * 不同类型信息更新频率不同；
  * 能避免旧状态与当前事实混杂。
* 未采用方案及原因：

  * 继续依赖长对话：上下文过长且容易遗漏；
  * 单一文件：长期事实、临时状态和决策过程难以维护。
* 依据：

  * 用户明确要求生成并区分三个文件；
  * 已完成事实审计和文档结构定义。
* 影响范围：

  * ChatGPT 项目迁移；
  * Codex 接手；
  * 文档维护；
  * 后续状态更新。
* 风险与代价：

  * 三份文件需要同步维护；
  * 如果不按职责更新，仍可能产生冲突。
* 可否逆转：

  * 可以，但不建议在当前迁移阶段改变。
* 重新评估条件：

  * 项目文档体系发生整体调整；
  * 有新的统一项目管理机制。
* 与其他决策的关系：

  * 为 D-015 的跨对话保存提供承载方式；
  * D-018 决定历史失败方案放入本日志。

### D-018 保留已失败或被替代的方案及其教训

* 状态：已确认
* 决策时间：本长对话阶段内，准确日期待确认
* 背景：

  * 项目经历了固定模板、单一最大跳变和结论遗漏等问题；
  * 用户希望保留这些过程作为面试素材。
* 需要解决的问题：

  * 交接文档是否只保存最终方案；
  * 被替代方案是否应删除。
* 可选方案：

  * 只记录最终有效方案；
  * 删除失败方案；
  * 保留具有解释价值的失败方案、原因和替代关系。
* 最终采用方案：

  * 保留中间失败方案及其教训；
  * 在 `DECISION_LOG.md` 中明确标记为“已替代”或“已否决”。
* 采用理由：

  * 能解释当前架构形成过程；
  * 能用于面试讲述问题发现、验证和修复能力；
  * 有助于避免后续重复采用已失败方案。
* 未采用方案及原因：

  * 删除失败方案：会丢失维护和面试价值；
  * 将失败方案写入当前有效状态：会造成误解。
* 依据：

  * 用户明确要求保留中间失败方案及教训。
* 影响范围：

  * 决策日志；
  * 项目复盘；
  * 面试材料。
* 风险与代价：

  * 文档长度增加；
  * 必须明确区分历史方案和当前方案。
* 可否逆转：

  * 可以，但不建议删除已有解释价值的记录。
* 重新评估条件：

  * 日志内容不再具有维护或复盘价值；
  * 出现隐私或不适合保留的信息。
* 与其他决策的关系：

  * 依赖 D-017 的文档职责划分；
  * D-006、D-008 和 D-010 是主要保留对象。

### D-019 下一阶段开发方向

* 状态：已确认
* 决策时间：2026 年 7 月 20 日
* 背景：

  * V4.2 站点任务版、综合 Excel/Word、月际解读质量评估和报告格式收尾已基本完成；
  * 当前初始产品已经具备真实数据处理、人工质控、统计分析和报告导出能力；
  * 用户希望下一阶段从继续扩展核心算法，转向提高可用性和实际使用范围。
* 需要解决的问题：

  * 当前版本完成后，下一阶段最值得投入的方向是什么；
  * 如何避免继续增加低收益的算法复杂度，同时让同事能够实际使用。
* 可选方案：

  * 继续优化月际解读算法；
  * 继续扩展新的分析功能；
  * 暂停代码开发，只整理求职材料；
  * 优先进行页面美化，再开展上线部署和同事试用。
* 最终采用方案：

  * 下一阶段分为两部分：
    1. Streamlit 页面美化和交互体验优化；
    2. 上线部署并邀请同事试用。
  * 页面美化先于正式部署实施；
  * 不在该阶段默认修改质控、统计和月际解读核心算法。
* 采用理由：

  * 当前核心业务闭环已经基本完成；
  * 月际算法评估表明主要事实和趋势已经可靠，继续调参的边际收益较低；
  * 页面体验和部署更能直接提升真实使用价值、同事复用能力和求职展示完整度；
  * 同事试用可以产生真实反馈，为后续是否继续产品化提供依据。
* 未采用方案及原因：

  * 继续默认优化月际算法：已有实验未产生稳定提升，且会增加维护复杂度；
  * 当前立即扩展新分析功能：容易再次扩大范围，且缺少明确业务需求；
  * 完全停止开发：无法验证同事使用和部署价值。
* 依据：

  * 用户明确提出下一阶段分为页面美化和上线部署两大部分；
  * V4.2 月际质量评估和稳定路线结论见 D-021。
* 影响范围：

  * 后续开发路线；
  * Streamlit 页面；
  * 部署方式；
  * 使用说明；
  * 同事试用和反馈；
  * 求职项目展示。
* 风险与代价：

  * 页面美化范围尚未拆解，容易发生无边界调整；
  * 部署涉及数据隐私、访问控制、文件存储和维护责任；
  * 未确定部署方案前，不应直接上传真实监测数据到外部服务。
* 可否逆转：

  * 可以。
* 重新评估条件：

  * 页面美化或部署成本明显超过预期；
  * 同事试用表明核心流程仍存在阻断问题；
  * 出现新的明确业务需求或数据安全要求。
* 与其他决策的关系：

  * 遵循 D-001 的项目边界；
  * 建立在 D-014、D-015 和 D-021 的算法收尾与稳定基线之上；
  * 页面版本标识仍由 D-020 继续约束。

### D-020 Streamlit 页面版本标识采用当前稳定版本名称

* 状态：已确认
* 决策时间：2026 年 7 月 20 日
* 背景：

  * 早期 Streamlit 页面曾显示“当前版本：V3 通用多变量版”，与后续 V4.1.1、V4.2 实际功能状态不一致；
  * 当前 `app.py` 已显示“V4.2 站点任务版”，旧的“页面仍显示 V3”事实已失效；
  * 页面版本标识需要与当前稳定产品阶段一致，避免同事试用和项目展示时产生误解。
* 需要解决的问题：

  * 页面应如何表达当前产品版本；
  * 如何避免继续保留过期版本文字。
* 可选方案：

  * 继续显示旧的 V3 名称；
  * 只显示不带版本号的通用产品名称；
  * 显示当前稳定版本名称“V4.2 站点任务版”。
* 最终采用方案：

  * 当前 Streamlit 页面显示“V4.2 站点任务版”；
  * 不再保留“V3 通用多变量版”的旧版本标识；
  * 后续正式冻结新版本时，应同步核对页面版本文字、项目文档和 Git 版本状态。
* 采用理由：

  * 当前页面文字已经与 V4.2 站点任务功能相匹配；
  * 明确版本名称便于内部试用、问题反馈和项目演示；
  * 可避免页面展示与代码、文档状态不一致。
* 未采用方案及原因：

  * 继续保留 V3：事实已经过时，会造成版本误解；
  * 仅使用通用产品名称：可以作为未来品牌化方案，但当前仍需要清晰标识稳定版本。
* 依据：

  * 用户明确确认当前 `app.py` 已显示“V4.2 站点任务版”；
  * 当前稳定路线为 V4.2 站点任务版。
* 影响范围：

  * Streamlit 页面；
  * 版本展示；
  * 项目演示；
  * 同事试用时的问题定位。
* 风险与代价：

  * 后续版本升级时需要同步修改页面版本文字；
  * 若只修改页面而未同步文档和标签，仍可能产生版本不一致。
* 可否逆转：

  * 可以。
* 重新评估条件：

  * 项目形成正式产品名称；
  * 页面不再展示具体版本号；
  * 发布新的稳定版本。
* 与其他决策的关系：

  * 不改变 D-015 的 Git 完成基线；
  * 与 D-019 的页面美化和部署方向直接相关；
  * 页面版本升级时应同步更新 `CURRENT_STATUS.md`。

### D-021 基于人工盲标评估保留 V4.2 月际叙述算法

* 状态：已确认
* 决策时间：2026 年 7 月 20 日
* 背景：

  * V4.2 月际叙述算法能够生成多阶段变化描述，但部分稳定序列存在过度分段和文字冗余；
  * 曾尝试增加月际叙述压缩规则，代码量和复杂度明显增加，但结果没有稳定改善；
  * 需要依据真实评估结果决定是否继续修改算法。
* 需要解决的问题：

  * 当前月际算法是否已经达到可用水平；
  * 是否继续增加规则以压缩稳定序列的叙述；
  * 如何在准确性、可维护性和开发投入之间取舍。
* 可选方案：

  * 继续增加稳定阈值和阶段合并规则；
  * 引入更复杂的统计方法；
  * 接入大模型进行月际叙述；
  * 保留 V4.2 稳定算法，将轻微冗余交由人工快速审阅。
* 最终采用方案：

  * 保留 V4.2 原有月际阶段识别与叙述算法；
  * 不合并月际叙述压缩实验；
  * 不继续默认开发压缩算法；
  * 已知的稳定序列过度分段问题通过报告导出前人工快速审阅控制。
* 采用理由：

  * 已基于北海、威海黄海靖和西霞口三个站点、九个变量建立 27 个样本；
  * 已完成 development / holdout 分组、人工盲标和基线复核；
  * Development：`direct_accept` 44%，`acceptable` 83%，`usable_after_minor` 100%；
  * Holdout：`direct_accept` 33%，`acceptable` 100%，`usable_after_minor` 100%；
  * 27 个样本中没有 `major_revision` 或 `incorrect`，未出现主要趋势或核心事实错误；
  * 剩余问题主要是稳定模式被拆成多次小幅升降，属于叙述冗余而非事实错误；
  * 压缩实验未提高评估结果，却增加代码和维护成本。
* 未采用方案及原因：

  * 继续堆叠规则：容易过拟合当前样本，并可能将真实阶段过度合并；
  * 立即引入复杂统计方法：当前没有由主要趋势错误驱动的必要性；
  * 接入大模型：不能解决底层事实问题，并增加隐私、成本、非确定性和校验负担。
* 依据：

  * 三站点、九变量、27 样本的人工盲标和基线评估结果；
  * 月际压缩实验的开发集与 holdout 结果未获得稳定改善；
  * 用户确认当前结果可接受，并决定停止继续消耗精力调参。
* 影响范围：

  * `src/time_series_interpretation.py`；
  * `src/report_context.py`；
  * 单变量和综合 Word 的月际文字；
  * 月际评估工具；
  * 后续算法开发优先级；
  * 项目面试表述。
* 风险与代价：

  * 稳定序列仍可能出现较多变化句或单月句；
  * 27 个样本不能代表所有站点和年份；
  * 正式导出前仍需要快速人工审阅。
* 可否逆转：

  * 可以，但必须在新分支中进行，并重新使用 development / holdout 评估。
* 重新评估条件：

  * 新站点数据出现可重复的主要趋势或核心事实错误；
  * 同事试用表明冗余文字严重影响报告使用；
  * 获得更大规模、独立的人工标注数据集；
  * 新方法能够在不显著增加复杂度的情况下改善 holdout。
* 与其他决策的关系：

  * 延续 D-002 的确定性报告路线；
  * 建立在 D-011 的通用多阶段识别之上；
  * 进一步落实 D-014 的范围控制；
  * 为 D-019 转向页面美化和部署提供算法收尾依据。

## 待确认决策

1. **页面美化的具体范围和优先级**

   * 已确认下一阶段先进行页面美化；
   * 尚未确认需要调整的页面、组件、视觉风格和验收标准。

2. **部署方式与数据安全方案**

   * 已确认后续需要上线部署并邀请同事试用；
   * 尚未确定使用本地局域网、云服务器、容器化部署或桌面打包；
   * 尚未确定真实监测数据是否允许上传远程服务器、文件保留期限和访问权限。

3. **项目后续定位的优先级**

   * 求职展示优先；
   * 课题组长期复用优先；
   * 两者并行；
   * 当前已确认同事试用，但长期维护和正式业务定位仍待确定。

4. **Streamlit 页面版本标识方式**

   * 使用具体版本号；
   * 使用不易过期的通用产品名称；
   * 应在页面美化阶段完成决策。

5. **未来是否重新讨论大模型润色**

   * 当前不接入，也不列为既定路线；
   * 是否在事实层长期稳定后增加可关闭的润色层仍待定。

6. **同事试用范围和反馈机制**

   * 尚未确定首批试用人数；
   * 尚未确定反馈表、错误日志、版本更新和维护责任。

## 已失效但应保留的决策

1. **固定模板加少量模式句子的报告方案**

   * 对应 D-006；
   * 已被三尺度事实组织方案替代；
   * 保留原因：说明报告为何曾出现变量间文本雷同。

2. **图 2、图 3、图 4 分别与对应分析段落交替排列**

   * 对应 D-008；
   * 已被先写综合分析、再连续放置图件的结构替代；
   * 保留原因：说明当前排版是用户明确选择的结果。

3. **只寻找一个最大月际跳变**

   * 对应 D-010；
   * 已被通用多阶段方向识别替代；
   * 保留原因：该方案曾导致先降后升、单月区间和方向延续错误。

4. **结论只取第一阶段或固定拼接极值月份**

   * 已由 D-012 替代；
   * 保留原因：说明水深周期和后期变化曾被遗漏的原因。

5. **当前阶段立即接入大模型 API**

   * 未成为实际执行方案，当前已明确不采用；
   * 保留原因：说明确定性报告路线的技术边界和取舍。

6. **月际叙述压缩实验**

   * 对应 D-021；
   * 实验通过增加稳定识别和阶段合并规则尝试减少碎片化；
   * 未在 development 和 holdout 上形成稳定改进，因此不合并到稳定路线；
   * 保留原因：说明项目如何依据评估结果停止低收益算法迭代。

# 未写入的普通建议

* 在下一次生成 Word 时顺手检查新 Git 标签是否显示正确；
* 将千位分隔和百分比格式统一；
* 将 Streamlit 页面上的版本文字改为更准确的名称；
* 列出 10 项轻量测试的具体测试名称；
* 生成内部验收表对比月平均路径和报告路径；
* 对具体报告中的个别句式进行人工润色；
* 将某个具体月份的描述改为更自然的表达；
* 在下一次正常提交中顺带处理低优先级格式问题；
* 为新对话提供简洁启动提示词；
* 将项目经历整理成简历和面试表述；
* 重新生成一份 Word 检查附录标签；
* 对某一个变量单独查看图 4 和正文是否一致。

这些内容属于操作建议、验收动作或临时任务，不构成长期实质性决策。

# 因证据不足未能认定为决策的内容

1. **页面美化的具体设计方案**

   * 只确认页面美化是下一阶段方向，尚未确认具体页面、样式和验收标准。

2. **部署使用哪种技术方案**

   * 只确认需要上线并供同事试用，尚未比较局域网、云端、容器化或桌面打包方案。

3. **项目是否一定交由课题组长期使用**

   * 当前只确认具有实际工具属性，没有确认长期使用安排。

4. **项目是否彻底停止开发**

   * 用户只确认当前单变量算法可以收尾，没有确认整个项目永久停止。

5. **未来是否永久禁止大模型润色**

   * 当前只确认不作为既定路线，未来仍可重新讨论。

6. **是否必须修改当前分支名称**

   * 当前分支名称与版本阶段不同，但本窗口没有形成改名决定。

7. **旧 Word 报告显示 V3 标签的具体原因**

   * 可能与生成时间或读取逻辑有关，但尚未验证，不能认定为硬编码问题。

8. **10 项轻量测试的具体覆盖范围**

   * 只确认 `10 passed`，没有逐项测试名称。

9. **九个变量是否已经完成同一次代码状态下的完整回归**

   * 本窗口没有足够证据确认。

10. **Streamlit 页面最终应显示 `V4.1.1` 还是通用名称**

    * 尚未由用户决定。

11. **是否需要为低优先级格式问题创建独立版本**

    * 当前决定是延后处理，但未来具体以哪个版本完成尚未确定。

12. **正式部署的具体范围、权限和数据存储规则**

    * 已确认需要上线部署并供同事试用；
    * 具体部署方式、账号权限、数据存储和维护责任尚未形成决定。
